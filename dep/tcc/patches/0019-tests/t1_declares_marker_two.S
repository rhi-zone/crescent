/* Second declaring input, distinct symbol so it can be merged with the
   first without a duplicate-definition error. */
.text
.globl declares_probe_two
declares_probe_two:
	ret
.section .note.GNU-stack,"",%progbits
