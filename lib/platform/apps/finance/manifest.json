{
  "name": "finance",
  "version": "0.1.0",
  "meta": { "tags": ["finance", "bookkeeping", "ledger"] },
  "default_entry": "headless",
  "entry": {
    "headless": {
      "main": "init.lua",
      "caps": {
        "db": { "type": "db", "allow_write": true, "reason": "Source of truth for the chart of accounts, periods, and journal entries (lib.bookkeeping.store, via the finance bridge)." }
      }
    },
    "tui": {
      "main": "tui.lua",
      "caps": {
        "db": { "type": "db", "allow_write": true, "reason": "Source of truth for the chart of accounts, periods, and journal entries (lib.bookkeeping.store, via the finance bridge)." },
        "stdin": { "type": "stdin", "required": true, "reason": "Reads menu selections and form input from the terminal." },
        "stdout": { "type": "stdout", "required": true, "reason": "Renders the current screen to the terminal." }
      }
    },
    "dom": {
      "main": "dom.lua",
      "caps": {
        "db": { "type": "db", "allow_write": true, "reason": "Source of truth for the chart of accounts, periods, and journal entries (lib.bookkeeping.store, via the finance bridge)." },
        "server": { "type": "http_server", "required": true, "reason": "Serves server-rendered HTML pages and handles form submissions; this app renders no client-side JavaScript, so every page is generated and every action processed on the server." }
      }
    }
  }
}
