return {
	alpha = require("lib.type.framework.alpha"),
	canonical = require("lib.type.framework.canonical"),
	replay = require("lib.type.framework.replay"),
	shape = require("lib.type.framework.shape"),
}
